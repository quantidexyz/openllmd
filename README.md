# openllmd

Released binaries for the OpenLLM local daemon (openllmd).
Managed by `packages/daemon/scripts/release.ts` in the main repo.
Assets are attached to tagged releases; do not commit binaries here.
